# Harness Resources

Shared, reviewed resources projected into this repository's generated harness
targets. Edit files here, then run `pnpm harness:preview` before applying with
`pnpm harness:activate`.

Personal or experimental resources should live under `.harness/local/resources`
until they are ready to promote into this shared source root.
